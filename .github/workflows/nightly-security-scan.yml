# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: nightly-security-scan
'on':
  workflow_dispatch: {}
jobs:
  nightly_security:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Install tfsec
      run: 'curl -s https://raw.githubusercontent.com/aquasecurity/tfsec/master/scripts/install_linux.sh
        | bash

        '
    - name: Full security scan
      run: tfsec infra --format json --out tfsec-report.json || true
    - name: Install checkov
      run: pip install checkov
    - name: Full checkov scan
      run: checkov -d infra --output json --output-file-path /tmp/checkov-report ||
        true
    - name: Upload security reports
      uses: actions/upload-artifact@v4
      with:
        name: security-reports
        path: 'tfsec-report.json

          /tmp/checkov-report/results_cli.json

          '
