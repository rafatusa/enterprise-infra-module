# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: destroy
'on':
  workflow_dispatch: {}
jobs:
  destroy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: No infrastructure to destroy
      run: 'echo "This is an infrastructure module library repo."

        echo "No infrastructure is deployed from this repo directly."

        echo "Consumers own their own state and destroy their own resources."

        '
