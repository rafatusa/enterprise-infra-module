# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: nightly-scan
'on':
  workflow_dispatch: {}
jobs:
  deep-security:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Install tools
      run: pip install checkov
    - name: Full checkov scan
      run: 'mkdir -p reports

        checkov -d infra/modules

        '
    - name: Install tfsec
      run: 'wget -q -O /usr/local/bin/tfsec https://github.com/aquasecurity/tfsec/releases/download/v1.28.11/tfsec-linux-amd64

        chmod +x /usr/local/bin/tfsec

        '
    - name: Full tfsec scan
      run: tfsec infra/modules
  generate-docs:
    runs-on: ubuntu-latest
    needs:
    - deep-security
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Install terraform-docs
      run: 'wget -q https://github.com/terraform-docs/terraform-docs/releases/download/v0.18.0/terraform-docs-v0.18.0-linux-amd64.tar.gz

        tar -xzf terraform-docs-v0.18.0-linux-amd64.tar.gz

        mv terraform-docs /usr/local/bin/

        '
    - name: Generate AWS module docs
      run: "for mod in infra/modules/aws/*/; do\n  terraform-docs markdown table --output-file\
        \ README.md \"$mod\"\ndone\n"
    - name: Generate Azure module docs
      run: "for mod in infra/modules/azure/*/; do\n  terraform-docs markdown table\
        \ --output-file README.md \"$mod\"\ndone\n"
